run:
  name: demo_cube_store_ema_long_swing
  out_dir: runs
  seed: 7
  log_level: INFO
  progress_every_ticks: 376

modules:
  market_feed: ../market_feed/cube_feed.yaml
  execution:   ../execution/paper_fixed_bps.yaml
  strategy:    ../strategy/ema_long_swing.yaml
  ui:          ../ui/qt_dashboard.yaml

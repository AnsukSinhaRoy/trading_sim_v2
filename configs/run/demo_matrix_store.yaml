run:
  name: demo_matrix_store
  out_dir: runs
  seed: 7
  log_level: INFO
  progress_every_ticks: 250

modules:
  market_feed: ../market_feed/matrix_store_1m.yaml
  execution:   ../execution/paper_fixed_bps.yaml
  strategy:    ../strategy/toy_rebalance.yaml
  ui:          ../ui/live_dashboard.yaml
